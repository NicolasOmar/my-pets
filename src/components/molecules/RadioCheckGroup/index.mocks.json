{
  "testing": {
    "minimalConfig": {
      "control": "minimalGroup",
      "name": "minimal-group",
      "type": "radio",
      "options": [
        {
          "type": "radio",
          "control": "optionRadio1",
          "label": "Option 1"
        },
        {
          "type": "radio",
          "control": "optionRadio2",
          "label": "Option 2"
        },
        {
          "type": "radio",
          "control": "optionRadio3",
          "label": "Option 3"
        }
      ]
    }
  },
  "storybook": {
    "options": {
      "description": "*Required*. Sets the options that will be shown inside this group"
    },
    "type": {
      "description": "*Required*. Sets the type for all the controls inside the group"
    },
    "name": {
      "description": "*Required*. Used to indicate in the `onInputChange` and `onBlurChange` which input has been affected after executing that function"
    },
    "value": {
      "description": "Indicate which of the displayed options has been selected. Can be one in a single string or several in an array of strings (as well a boolean case for the RadioButton case)"
    },
    "onInputChange": {
      "description": "Sends event and control information to its parent component when user press a new key"
    },
    "onBlurChange": {
      "description": "Sends control information to its parent component when user changes its focus to other input"
    }
  }
}
