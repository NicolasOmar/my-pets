export enum buttonType {
  button = 'button',
  submit = 'submit',
  reset = 'reset'
}

export enum inputType {
  text = 'text',
  email = 'email',
  password = 'password',
  date = 'date',
  number = 'number'
}

export enum checkType {
  radio = 'radio',
  checkbox = 'checkbox'
}

export enum selectorType {
  select = 'select',
  multiple = 'multiple'
}
